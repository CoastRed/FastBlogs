<UserControl x:Class="FastBlogs.Views.SystemSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FastBlogs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="系统配置界面" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25" Foreground="#409eff"/>
        <WrapPanel Grid.Row="1" Margin="25">
            <Button Content="用户信息配置" Padding="15 10" Style="{StaticResource buttonStyle}" Width="Auto" Height="Auto"
                    Command="{Binding SetUserInfoCommand}"/>
        </WrapPanel>
    </Grid>
</UserControl>
